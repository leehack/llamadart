name: llamadart_basic_example
description: A basic example of using llamadart package
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  llamadart:
    path: ../..  # Use local version for testing
  http: ^1.6.0
  path: ^1.8.3
  args: ^2.4.2

dev_dependencies:
  lints: ^6.1.0
