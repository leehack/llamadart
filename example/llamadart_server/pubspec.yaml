name: llamadart_server
description: OpenAI-compatible API server example for llamadart
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  llamadart:
    path: ../..
  relic: ^1.2.0
  args: ^2.7.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.3
